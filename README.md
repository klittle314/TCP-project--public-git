# TCP-project--public-git
scripts for TCP Shiny app
The repository has the scripts that generate a functional Shiny app.   The Google Analytics token file is not included to preserve client site security.
